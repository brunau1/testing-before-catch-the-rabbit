export interface IConnectionConfiguration {
  frameMax: number;
  heartbeat: number;
  hostname: string;
  locale: string;
  protocol: string;
  username: string;
  password: string;
  port: number;
  vhost: string;
}
